spring.application.name=${SPRING_APP_NAME:sarh}

# ===============================
# MULTIPART
# ===============================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=${MULTIPART_MAX_FILE_SIZE:5MB}
spring.servlet.multipart.max-request-size=${MULTIPART_MAX_REQUEST_SIZE:10MB}

# ===============================
# MEDIA
# ===============================
media.location=${MEDIA_LOCATION:mediafiles}

spring.main.allow-circular-references=true

# ===============================
# DATASOURCE
# ===============================
#spring.datasource.url=jdbc:mariadb://${PGHOST}:${PGPORT}/${PGDATABASE}
#spring.datasource.url=jdbc:mariadb://${DB_HOST}:${DB_PORT}/${DB_NAME}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}

# ======================================================
#   AUTH
# ======================================================
#spring.datasource.auth.url=jdbc:mariadb://${DB_AUTH_HOST}:${DB_AUTH_PORT}/${DB_AUTH_NAME}
#spring.datasource.auth.username=${DB_AUTH_USERNAME}
#spring.datasource.auth.password=${DB_AUTH_PASSWORD}
#spring.datasource.auth.driver-class-name=org.mariadb.jdbc.Driver

# =======================================================
#   PROD
# =======================================================
#spring.datasource.url=jdbc:mariadb://${DB_PROD_HOST}:${DB_PROD_PORT}/${DB_PROD_NAME}
spring.datasource.url=jdbc:postgresql://${PG_HOST}:${PG_PORT}/${PG_NAME}
spring.datasource.username=${PG_USERNAME}
spring.datasource.password=${PG_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

#spring.datasource.driver-class-name=org.mariadb.jdbc.Driver


# ========================================================
# TEST
# ========================================================
#spring.datasource.test.url=jdbc:mariadb://${DB_TEST_HOST}:${DB_TEST_PORT}/${DB_TEST_NAME}
#spring.datasource.test.username=${DB_TEST_USERNAME}
#spring.datasource.test.password=${DB_TEST_PASSWORD}
#spring.datasource.test.driver-class-name=org.mariadb.jdbc.Driver


#spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
#org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:create}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===============================
# JACKSON
# ===============================
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

# ===============================
# MAIL
# ===============================
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=false

mail.from=${MAIL_FROM}

# ===============================
# JWT
# ===============================
jwt.secret.key=${JWT_SECRET_KEY}
jwt.user.generator=${JWT_USER_GENERATOR}
jwt.time.expiration=${JWT_EXPIRATION}
jwt.time.refreshExpiration=${JWT_REFRESH_EXPIRATION}
